/**
 * OJ 1024
 *
 */

#include <iostream>

using namespace std;

#define DEBUG1 1

int main ()
{
#if DEBUG1
    freopen("1024.in", "r", stdin);
#endif
    int cities, roads;
    cin << cities << roads;
    map = new int[cities][cities];
    for (int i = 0; i < roads; ++i)
    {
        /* code */
    }
    return 0;
}